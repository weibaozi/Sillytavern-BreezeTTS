# 酒馆 × Breeze TTS 实施计划 · 0.7.0-tag-render.13

本次 0.7.0-tag-render.13：新增多规则文本过滤，默认将省略号「……」替换为空格；角色和旁白合成统一使用过滤后的文本，全空内容跳过请求。

前版 0.7.0-tag-render.12：新增随聊天保存的旁白获取方式：不获取、播放时获取、自动获取；新旧聊天默认播放时获取。

前版 0.7.0-tag-render.11：默认预设最后的示例人物改为 Bob；已保存的原版默认模板自动更新，自定义模板保持不变。

前版 0.7.0-tag-render.10：新增可展开／收起的浮动语音面板与整条回复进度拖动；支持补齐音频后定位、暂停定位和按回复选择。

前版 0.7.0-tag-render.9：新增本条语音重新获取（强制替换缓存、支持停止和进度）；旁白跨换行累计目标字数，普通引号与引用内容纳入朗读。只需更新插件。

前版 0.7.0-tag-render.4：增加按聊天保存的旁白音色与情绪，默认「平稳口气，配音」。整条/自动播放及自动预生成按正文顺序处理旁白与对白；旁白不进入预设。流式等待完整片段，续写跳过旧旁白，停止或切换聊天取消队列。

状态：`main` 延续原 `experiment/ttsvoice-render`，已实现单份 TTSVoice 渲染；前版已安装，本次新增读取流式文本与原生引号主题样式。稳定分支 `stable/v0.6.1` 与标签 `v0.6.1` 保留在 `60df1f8`。2026-09-19。

工作区：TTS 与本插件独立维护；维护者本地保留旧稳定版工作区，当前插件在 `main` 发布。仓库根目录符合酒馆 Git 安装布局，npm 仅用于开发测试。安装步骤见 [README](README.md)。

## 本次更新：流式文本与对白样式

- 输出过程中实时隐藏未闭合标签的元数据，展示已经到达的对白；原始消息保持不变，未闭合内容不会合成。
- 新增「读取流式文本」设置，完整标签到达后按自动生成／自动播放开关追加到顺序队列；关闭后仍即时渲染，但自动语音等待整条回复结束。
- 与「流式生成与播放」音频开关独立；停止、编辑、切换回复及聊天会撤销旧队列。续写只处理新增或补完的标签。
- 对白使用原生 q 元素及酒馆引号主题色，默认橘黄色。

以下单份对白基础实现及验证数字记录 0.7.0-tag-render.1；最新使用与验证见 README。

## 本次实验：单份对白

- 模型仅输出 `[TTSVoice:角色名:情绪:对白]`；显示层呈现普通引号对白与气泡，原始 `mes` 保持不变。未绑定人物也能读到对白，只跳过合成。
- 当前配置与内置默认语气词（支持 `[]`／`()`）仅在显示时移除，未知括号保留，发送 TTS 时保留原发声标签。
- 旧版紧邻段落的引号全文精确匹配时省略重复显示，属于保守启发式，不对任意旧格式作迁移保证。
- 默认五条英文规则及可选第六条聊天语料保留；实验模板使用 `tagRenderPromptTemplate`，稳定的 `settings.promptTemplate` 不覆盖。其余设置继续共用。
- 新显示模块为 `dialogue-render.js`，实验包名 `dist/sillytavern-breeze-0.7.0-tag-render.1.zip`。安装需显式 `-InstallExperimental`，不要与稳定版并装；本次未执行实际安装。
- 70 项单元／DOM 测试、35 项不同浏览器用例和语法检查通过。浏览器使用模拟酒馆与音频；桌面及 375px 手机截图已目视检查。安装 guard 和 `-WhatIf` 已验证，稳定目录和真实酒馆原运行文件一致；真实模型遵循率尚未验收。
- 具体实现、TGbreak 新片段与回退限制见 [单份对白方案](docs/单份对白方案.md)。

以下第 1–11 节保留 **0.6.1 及之前的原实施计划和历史记录**；其中双份对白、旧安装及验证数字描述的是稳定版，不是本实验当前状态。

## 1. 目标与已确认的规则

让 SillyTavern 的模型回复自动生成角色语音，并在对话旁显示可点击播放的气泡。

- 插件读取 `[TTSVoice:角色名:情绪:对话内容]`；0.1.1 起由插件动态注入可编辑英文规则，保留手动管理方式与独立注入开关。
- 文本中的 `[笑]`、`[叹气]` 等原样传给 Breeze。项目 README 已说明模型支持中文方括号语气标签，不转成朗读文本。
- 本地 TTS 保存音色 profile：稳定 ID、名称、参考音频、准确的对应文字、可选音色描述。
- 每个聊天独立保存「对话角色 → 音色 ID」映射，多个角色可共用一个音色。
- 初始映射为空。未映射角色跳过，不自动按同名音色匹配，也不随机选音色。
- 角色来源：当前角色卡、当前群聊成员、当前聊天消息里的 TTSVoice 标签；支持手动补充角色名。
- 排除用户消息、系统消息及当前用户 persona 对应的角色名。
- 在酒馆内完成音色选择、上传参考音频、设计候选音色、试听和保存。
- 先做独立的 SillyTavern UI 扩展，不依赖酒馆助手，也不修改其 JSON 插件。

音色 profile 与酒馆人物设定分开：不需要手工录入完整人物设定，只在需要设计新音色时输入声音描述。

## 2. 完整使用流程

### 建立音色库

1. 启动 Breeze WebUI 并加载模型。
2. 打开酒馆输入栏左下角的魔棒菜单，点击「Breeze 语音工作室」，进入独立管理面板。
3. 连接 TTS 地址并检查状态。
4. 从以下三种方式增加音色：
   - 上传参考音频，填写准确的对应文字和音色名称。
   - 填写声音描述、测试文本、候选数量 k，生成不同 seed 的候选音频。
   - 在「声音设计室」选择「语音方向」，上传参考音频与准确文字，填写新试听文本和可选方向描述，生成不同 seed 的候选。
5. 每个候选提供试听与保存按钮；选中后输入名称，保存至 TTS 本地。
6. 候选的参考文字直接使用生成时的测试文本，避免音频和文字错配。试听确认文本正确后再保存。

也可直接在 WebUI 完成生成、试听、保存或导入；「合成 → 已保存音色」读取共用音色库，酒馆刷新后可绑定新音色。WebUI「设置」保存自动加载模型、默认加速、自动打开网页、监听与默认合成参数；启动项下次重启生效，模型加载参数需重新加载。

### 绑定聊天角色

1. 在「角色与音色」页列出角色，初始全部是「未映射／跳过」。
2. 为角色选择音色，例如 `赵岚 → 温柔女声 B`。
3. 另一个角色也可以选择同一个音色 B。
4. 映射写入当前聊天 metadata；换聊天时切换映射，不影响其他聊天。
5. 新角色出现在标签中时自动加入列表，等待用户绑定。

### 对话播放

模型输出示例：

```text
赵岚说：“你好呀，赵岚。”
[TTSVoice:赵岚:轻快、带笑意:[笑]你好呀，赵岚。]
```

处理链：

```text
回复完成 → 解析标签 → 排除用户角色 → 查当前聊天映射
→ 未映射则跳过 / 已映射则提交任务
→ TTS 参考音频＋参考文字＋本句情绪＋含语气词的文本
→ 返回 WAV → 对话旁显示气泡 → 按原文顺序播放
```

第一版等待一条模型回复完成后再处理，不在半截流式标签出现时发请求。

## 3. 插件界面

采用酒馆原生扩展入口和独立管理面板，支持窄屏。

### 连接与播放

- API 地址、连接测试、模型加载状态。
- 启用开关、自动播放开关、音量、停止队列。
- 自动生成、自动播放默认关闭；若浏览器禁止自动播放，显示提示并允许点击气泡继续。
- 错误显示在对应气泡或面板中，避免不断弹窗。

### 当前聊天

- 显示当前聊天和汇总后的角色列表。
- 每个角色卡：角色名、音色下拉框、已绑定音色的直接试听；完整参考文本与试听位于「音色库」。
- 手动添加别名角色；不做含糊的模糊匹配。
- 未映射、新发现角色、引用音色已不存在等状态明确可见。

### 音色库

- 列表展示名称、参考文本、参考音频试听。
- 上传已有参考音频及文字。
- 设计新音色：纯文字设计，或上传参考音频与准确文字的语音方向；后者描述可留空。均支持测试文本、候选数量（1–8）、开始／取消。
- 候选显示序号、seed、生成状态、播放器、保存按钮。
- 候选依次生成，完成一个就可以试听，不必等全部完成。
- 已保存音色使用稳定 ID；同名保存提示改名，不静默覆盖。

### 消息中的气泡

- 在对应 TTSVoice 标签的位置替换为气泡，隐藏原标签；标签通常紧跟原对白。
- 无法可靠定位时，在本消息下方显示带角色名和短文本的语音条，保证不会绑定到错误句子。
- 状态：未映射、排队、生成中、播放／暂停、时长、失败重试。
- TTS 标签只在显示层处理，不修改原始聊天消息和发给 LLM 的内容。
- 同一句出现多次时按出现顺序匹配，避免所有气泡附在第一句。

## 4. 后端设计

保留原有 `POST /v1/audio/speech` 表单／PCM 接口。为本功能增加专用 `/breeze/*` 接口；这一阶段不追求 OpenAI SDK 兼容，因为聊天映射、参考音色库和候选管理需要额外协议。

WebUI 与独立 API 入口共用音色服务模块和同一套存储格式。用户启动 WebUI 即可使用；不需要为插件再加载一份模型。

### 接口约定

| 接口 | 用途 |
| --- | --- |
| `GET /breeze/health` | 模型是否加载、排队数量、协议版本 |
| `GET /breeze/voices` | 列出音色 ID、名称、文字、试听地址 |
| `GET /breeze/voices/{id}` | 获取一个音色的完整记录 |
| `POST /breeze/voices` | 上传名称＋参考音频＋对应文字 |
| `GET /breeze/voices/{id}/audio` | 试听参考音频 |
| `POST /breeze/jobs` | 提交对话合成或单个音色设计任务，立即返回任务 ID |
| `POST /breeze/jobs/direction` | multipart 上传 audio、ref_text、text 和可选 instruction、cfg_scale、seed，创建方向候选 |
| `GET /breeze/jobs/{id}` | 查询 queued/running/done/error/cancelled |
| `DELETE /breeze/jobs/{id}` | 取消等待任务；已进入模型的任务结束后丢弃结果 |
| `GET /breeze/jobs/{id}/audio` | 获取已完成任务的 WAV |
| `POST /breeze/voices/from-job` | 把选中的 design / direction 候选保存为音色 profile |

对话请求示例：

```json
{
  "kind": "speech",
  "voice_id": "本地音色的稳定ID",
  "text": "[笑]你好呀，赵岚。",
  "emotion": "轻快、带笑意",
  "instruction": "",
  "cfg_scale": 4,
  "seed": 42
}
```

设计候选请求示例：

```json
{
  "kind": "design",
  "text": "你好，很高兴见到你。今天想聊些什么？",
  "instruction": "年轻女性，声音温柔清澈，音调适中，语速自然。",
  "seed": 1001,
  "cfg_scale": 4
}
```

插件通过不同 seed 提交 k 个设计任务；参考方向每个候选独立提交 multipart。候选保存采用任务的生成文本 `text`，不采用上传参考的 `ref_text`。对话合成必须提供有效音色 ID；不因 ID 错误退回随机音色。

### 推理与并发

- 单 GPU 保持单任务推理，新请求进入有上限的 FIFO 队列。
- 新队列与原有 WebUI／API 共用推理锁，避免同时调用模型。
- 模型卸载也必须与推理互斥，避免任务中途释放模型。
- 插件的播放队列与生成队列分离：可以播放上一句的同时准备下一句，但声音不重叠。
- 任务失败显示原因，释放锁，后续任务继续；队列满返回 429，模型未加载返回 503。
- 长时间等待有超时及取消路径。失败重试由显式操作触发，避免无限重试。

### 文件与缓存

存储结构：

```text
data/tts/
  voices/<voice-id>.json
  voices/<voice-id>.wav
  jobs/<job-id>.json
  jobs/<job-id>.wav
  jobs/<job-id>.reference.wav  # 参考方向的临时输入
```

- metadata 原子写入，文件名使用 ID，不直接用人物名构造路径。
- 音频上传限制体积并验证格式、时长及非静音。
- 完成的 WAV 和任务信息可以在重启后读取；未完成任务标记中断。
- 方向参考 WAV 在完成／失败后删除；取消排队时立即删除，取消推理中任务则在推理退出后删除并丢弃结果；重启清理遗留临时参考，音色库与完成候选保留。
- 聊天气泡缓存键包含文本、情绪、音色 ID、生成参数和 TTS 地址；改映射后不能复用旧音色。
- 第一版保留已生成文件以便重播，文档说明磁盘增长及清理方法；不自动删除音色库。

## 5. 标签解析与聊天生命周期

### 标签解析

- 只把最前面的两个冒号作为角色、情绪分隔符；正文中的冒号保留。
- 用方括号嵌套计数解析外层标签，支持 `[TTSVoice:角色:情绪:[笑]你好[叹气]]`。
- 兼容字段两侧空白及中文全角冒号。
- 角色名与情绪字段不允许使用分隔冒号；正文可以。
- 未闭合、缺字段、空文本的标签跳过并提供诊断，不请求错误音频。
- 不对英文或中文语气词做未经确认的转换。

### 生命周期

- 新回复：去重后按标签顺序生成及自动播放。
- 历史消息加载：恢复气泡，不自动重播全部历史；点击时获取缓存或生成。
- 编辑／切换 swipe：停止关联播放，按新内容重建气泡，旧请求返回不得覆盖新内容。
- 切换聊天：停止旧聊天播放，取消尚未开始的旧任务；异步返回检查聊天标识后再写入。
- 手动点击气泡：当前同一条可暂停／继续；选择另一条先停当前条，避免重叠播放。
- 网络断开、模型未加载、音色失效：保留文本与失败状态，修复后允许重试。
- 插件异步合成不阻塞酒馆的消息事件处理。

## 6. 安装与连接

扩展源码位于本仓库根目录，支持酒馆通过 Git URL 安装；也提供 ZIP 和安装脚本。npm 依赖仅用于开发测试，不需要复制到酒馆。

用户本地安装酒馆后，将扩展复制到：

```text
SillyTavern/data/default-user/extensions/sillytavern-breeze/
```

其他用户使用各自的用户目录。第一版不需要额外酒馆服务端插件。

连接地址由运行酒馆页面的浏览器访问：

- 浏览器与 TTS 在本机：默认 `http://127.0.0.1:7860`。
- 浏览器在其他设备：使用 TTS 电脑的局域网地址，并配置监听地址及允许的酒馆 Origin。
- 后端默认允许 localhost／127.0.0.1 的酒馆页面跨域访问；其他来源通过 `BREEZE_TTS_ORIGINS` 显式配置。
- HTTPS 酒馆连接 HTTP TTS 可能被浏览器拦截；联调时按实际部署方式配置同协议或反向代理。

维护者的本地测试酒馆访问地址 `http://127.0.0.1:8002/`。0.2.0 的工作室入口已验证，完整语音联调仍待完成。

## 7. 分阶段实施与验收

### 阶段 A：音色库与队列 API

实施：共享服务模块、参考音频保存、设计与对话任务、WAV、CORS、WebUI／独立 API 接线、推理互斥。

验收：

- 能上传音频与文字，重启后仍能列出并试听。
- 以音色 ID 合成含 `[笑]` 的文本，实际返回可播放 WAV。
- 并发提交两个任务依次完成，不因内部互斥直接报 409。
- 未加载模型、未知音色、非法文件及取消路径有明确返回。
- 原有表单接口和 WebUI 仍能使用。

### 阶段 B：插件对话闭环

实施：连接面板、角色发现、聊天映射、解析器、消息气泡、自动播放及重播、缓存与生命周期处理。

验收：

- 一条回复包含两个角色和多个嵌套语气标签，解析完整。
- 已映射角色依次播放，未映射角色及用户角色跳过。
- 多个角色绑定同一个音色正常工作。
- 两个聊天对同一角色设置不同音色，互不影响。
- 历史加载不突然播放；切聊天、编辑和 swipe 不播放过期结果。
- 点击气泡可以重播，错误可见，浏览器播放权限可恢复。

### 阶段 C：酒馆内设计音色

实施：声音描述或参考音频与准确文字、可编辑测试文本、k 个候选、渐进试听、命名保存及刷新音色列表。0.3.0 补齐参考方向模式，与 WebUI 共用音色库。

验收：

- 生成 3 个不同 seed 候选，在面板中逐一试听。
- 保存第 2 个后，音频和参考文字均来自第 2 个候选。
- 新音色可立即绑定角色，后续聊天通过参考音频合成。
- 取消不会破坏已完成候选，失败不污染音色库。
- 参考方向上传的音频可试听，保存后的参考文本匹配新生成的候选，而非旧参考内容。

### 阶段 D：本地酒馆联调与交付

实施：在用户安装的酒馆中跑完整流程，修复版本／DOM／事件兼容问题，打包扩展和使用文档。

验收：

- 实际角色卡、群聊和聊天标签的角色发现正常。
- 真实回复自动生成音频，气泡位置正确，点击可重播。
- 刷新页面、重启 TTS 后音色和聊天映射仍然保留。
- 提供安装 ZIP、API 说明、操作步骤，以及已验证与未验证项。

## 8. 测试方式与范围

- Python：用假合成器验证持久化、音频格式校验、排队、取消、错误恢复和接口行为，避免每个测试都加载模型。
- JavaScript：验证嵌套标签、缺字段、重复文本、中文冒号、用户排除、映射和缓存失效。
- 真模型：音色设计 → 保存 → 情绪方向合成的端到端验证。
- 真酒馆：扩展安装、事件绑定、DOM 气泡、自动播放限制和聊天切换联调。
- 声音一致性及情绪、语气词效果需要试听评估；参考音频提供相对一致性，不承诺完全相同的音色或每次都产生指定事件。

暂不包含：OpenAI 全兼容层、从人物设定自动推断声音、未映射角色的自动分配、跨设备音色同步、GPU 多路并行推理、回复尚未完成时逐词朗读。

## 9. 待评估功能（暂不纳入必做范围）

### 消息内的新角色快速绑定／跳过

2026-09-19 用户提出参考 EchoCore：在回复文本中识别新角色后，直接在对应位置提供「绑定音色」和「跳过」操作。先记录，后续决定是否实现。

- 候选识别方式：从消息的 TTSVoice 标记发现尚未配置的角色；是否支持无标记的普通正文识别，另行评估，不默认承诺自动识别人名。
- 「绑定音色」：就地打开音色选择，复用本地音色库，将映射保存到当前聊天；多个角色仍可绑定同一音色。
- 「跳过」：将角色标记为主动跳过，避免同一聊天反复提示；未绑定或主动跳过时均不请求语音。
- 待确定交互：跳过状态的保存与撤销入口、同一角色多次出现时的提示去重，以及绑定后是否为当前句补生成／播放。绑定不应默认触发历史对白自动连播。
- 仅改变显示与配置，不删除或改写原始对白，不改变当前已确认的「未映射角色跳过」原则。

参考：[EchoCore 新角色识别与快速绑定界面](https://github.com/haide-D/SillyTavern-EchoCore/blob/main/frontend/js/dom_parser.js)。

## 10. 可配置的语气词模块与聊天语料

0.5.0 已完成全局语气词列表，0.6.0 将聊天额外语料升级为具名共享库。语气词支持编辑、去重、清空及恢复默认，默认仍为 `[笑]`、`[叹气]`、`[咳嗽]`、`[清嗓子]`；规则和示例通过插槽实时更新。额外语料可新建、命名、复制和删除，各聊天独立选择语料 ID 及开关。启用有效且非空的语料时，在预设末尾追加第 6 条。旧内容打开聊天时迁移并保留备份。

默认列表已在 0.7.0-tag-render.6 扩展为 47 项，内置列表不是数量上限。具名语气词预设组仍按下列范围后续评估；自定义模板与 TGbreak 其他约束保留。

- 插件设置中管理语气词预设组，支持新增、编辑、启用／停用和切换；可保留多组预设，避免反复手改整份 prompt。
- 每项保存实际标签文本、使用场景说明和可选示例；中文标签原样保留，不因提示词使用英语而翻译。具体可用标签与合成效果需按 Breeze 模型实际能力验证。
- 将英文提示词拆为固定协议和可变语气词模块。固定协议继续规定 `[TTSVoice:角色名:情绪:对白]`、正文对白后追加、角色规则、非正文排除及最后两次发言检查；语气词模块按当前启用的预设生成可用列表、说明和示例。
- 修改或切换语气词预设后，插件更新对应的提示词注入内容，下一次生成使用新列表；不覆盖 TGbreak 的其他约束，也不修改历史消息。
- 语气词与整体情绪字段分开配置：前者是对白内具体发声事件，后者控制本句整体语气。保留按需使用、不强制每句添加、不把无声动作当发声标签的要求。
- 解析器不能把允许列表写死为四个标签；外层 TTSVoice 继续按方括号嵌套解析，启用的语气词原样传给 TTS。若显示层隐藏发声标签，使用同一配置，不能删除正文里所有方括号内容。
- 后续验收：新增一项或切换预设组后，实际发送的 prompt 中列表同步改变；不再残留固定四项的限制，且原协议、其他格式模块和历史消息保持不变。

## 11. 当前代码状态

- 阶段 A：音色库、任务队列、WAV、CORS 和两种启动入口已接通；新增队列与原推理、模型卸载共用锁。
- 阶段 B：扩展设置、角色发现、聊天映射、嵌套解析、显示层气泡、顺序播放、缓存与生命周期处理已实现。
- 阶段 C：上传参考音色、文字设计与参考方向的不同种子候选、试听及命名保存已实现。
- 阶段 D：安装脚本、ZIP、双工作区配置和文档已提供；0.6.1 已安装至本地酒馆，原插件备份在 `backups/20260919-191423-087/`。本次 61 项功能测试与 2 项新增模板迁移浏览器回归通过。真实酒馆的魔棒入口与工作室打开在上一版已验证。完整聊天语音流程及模型试听尚未完成。
- Python 全套 112 项测试通过，包含真正逐块响应、取消与完成 WAV 衔接；独立 WebUI 测试端口实测设置保存与刷新恢复、共享音色的读取和参考预览，未加载真实模型。
- 前端 61 项测试和 JavaScript 语法检查通过，另有 22 项 Edge 浏览器交互测试，包含流式播放、语气词实时更新、语料共享复用、独立聊天绑定、复制删除和旧内容迁移。后端测试使用假合成器，浏览器使用模拟酒馆上下文，不代表实际音色效果已验收。
- 前期英文预设和世界书已迁入本目录；当前已安装酒馆的预设未被覆盖，多组语气词管理与新角色就地绑定仍为待评估项。
- 本轮修改的是源文件；运行中的旧 WebUI 需要重启才会出现新接口。
- 0.1.1 参考 EchoCore，使用独立 `breeze_voice_protocol` 扩展提示词 key，默认 `IN_CHAT`、depth 1、SYSTEM；每轮生成前编译名单，关闭时只清空本 key，quiet 和用户代写跳过。可在插件内编辑模板及预览，TGbreak 已保存的预设和历史文本保持原样。
- 0.2.0 改为自有语音工作室，从魔棒菜单进入；Shadow DOM 隔离主题，桌面五页导航及手机横向导航，新增角色搜索和已绑定音色直接试听。保留映射、参考上传、候选生成、自动注入和播放设置。浏览器实测同时修复原请求器的 fetch 绑定问题。
- 0.3.0 在插件加入参考方向候选流程，后端增加方向上传任务及单音色详情；WebUI 加入已保存音色模式、生成试听后的创建入口、音色库导入和启动／默认参数设置。设置保存至 `configs/webui.json`（可由 `BREEZE_WEBUI_CONFIG` 指定），默认不自动加载模型、自动打开网页，加速默认关闭。
- 0.4.0 增加「连接与播放 → 流式生成与播放」，默认关闭。后端任务接受 `stream: true`，直接从模型分块输出 PCM，结束后保留完整 WAV；前端按序播放，完成后缓存，取消时关闭连接并清理未完成任务。生成中的 PCM 暂存磁盘，播放缓冲限制在约 30 秒，避免慢速客户端无限堆积音频。仍等待 LLM 回复完成后处理对白。
- 0.5.0 在预设注入页增加全局语气词列表和当前聊天的额外语料开关与输入框；保存后编译预览立即更新，不直接修改基础模板。通过默认迁移保留旧配置，同时避免更改列表后示例仍固定包含 `[笑]`。额外语料启用且非空才追加为第 6 条，不混入其他聊天。
- 0.6.0 将额外语料存为全局 `extraPromptPresets`（ID、名称、正文），聊天保存 `extraPromptId` 和开关。共享修改对已绑定聊天生效，复制可独立调整；改名保留 ID，删除后不回退注入旧文字。旧聊天文字按需迁移，保持原启用状态并保留备份。
- 0.6.1 核对真实聊天原文，确认有对白只存在于 TTS 标签，非渲染层删除且与额外语料无关。默认规范要求先输出完整可读对白再追加音频副本，并限定去引号等操作仅在副本内；检查对白与标签双向对应。自动升级已知旧默认，保留自定义模板和其他设置；未进行真实模型生成，遵循率需后续观察。

参考：

- [SillyTavern 官方 UI 扩展文档](https://docs.sillytavern.app/for-contributors/writing-extensions/)
- [官方 getContext 实现](https://github.com/SillyTavern/SillyTavern/blob/release/public/scripts/st-context.js)
- [官方事件定义](https://github.com/SillyTavern/SillyTavern/blob/release/public/scripts/events.js)
- 本项目 `README.md` 的 Vocal Events 和 Voice Direction 说明。
