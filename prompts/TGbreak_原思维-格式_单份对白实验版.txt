---
格式要求:
  rule: |
    - **最终输出必须按照本规则【3个步骤模块】进行！优先级为**最高**，错误则回复无效！**
    - 本要求最终输出格式，禁止出现不在${strict_format}里的任何内容；TTSVoice 明确属于正文内允许的程序标记，每次发言仅在标签内写一遍，由插件显示为引号对白；其情绪字段及语气词不受禁语气解释限制，程序元数据不计入正文字数。行动选项、吐槽、摘要等正文外模块不添加 TTSVoice。
    - 本次创作必须严格遵守以下格式规范，**所有标签名需正确拼写并完整闭合**；TTSVoice 是以 [TTSVoice: 开始、以 ] 结束的单行程序标记；内部 [笑] 等语气词的方括号应独立闭合
    - 严格按html注释符<!-- 1.正文前的格式 -->...**一共3个步骤模块，不得调换顺序或遗漏。**
    - 检索对话上下文中的所有格式要求（包括但不限于选择框、插图、音频等），一并执行。
  strict_format:
    basic: |
<!-- 1.正文前的格式 -->{{getvar::gs-randomstuff}}{{getvar::gs-bginfor}}
<!-- 2.正文 -->
${{{getvar::gs-draft}}用**中文**输出正文；每次非 {{user}} 角色的实际发言仅以 [TTSVoice:角色名:情绪:原话] 单独一行输出一份，不再重复写普通引号对白；旁白及动作保留在标签外；包含独立短句及最后两次发言，倒数第二次和最后一次非 {{user}} 发言必须分别有完整标签，确认无漏标后才进入正文后的格式}
<!-- 3.正文后的格式 -->{{getvar::gs-w2g}}{{getvar::gs-catsay}}{{getvar::gs-CEstuff}}{{getvar::gs-SexualScene}}{{getvar::gs-summary}}
    double_check:
      - 在输出任何内容之前，必须先执行以下检查：
      - 1.除了strict_format外，**还有其他格式要求藏在 <Story setting> 和 <interaction_record>中**，你必须反复检索，依次检查有没有下列的格式要求（禁止偷懒、无中生有）：
        - 正文有无标签包裹？
        - 选择框？
        - 插图？
        - 音频？
        - 状态栏？{{getvar::gs-mvuvar}}{{getvar::gs-mvugs}}
        - 特殊xml标签？（可能很多个，不可偷懒）
        - 特殊符号，例如括号`【】`等
        ...
      - 2.将这些格式按出现顺序插入到 strict_format.basic 的对应位置。
      - 3.如果发现任何格式要求未被包含在 strict_format.basic 中，必须补充进去。
**未执行此检查直接输出正文，视为严重违规，回复无效**