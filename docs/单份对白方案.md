# 单份对白方案 · 0.7.0-tag-render.1

稳定分支 `stable/v0.6.1` 和标签 `v0.6.1` 保留在基线 `60df1f8`。实验分支为 `experiment/ttsvoice-render`，工作区位于 `G:\study\AI\breeze-tts\sillytavern-breeze\branches\ttsvoice-render`。稳定插件仍在 `G:\study\AI\breeze-tts\sillytavern-breeze`，后端仍在 `G:\study\AI\breeze-tts`。本次没有安装到真实酒馆，也没有更改后端。

## 单份输入与显示

```text
周启明拍了拍肚子。
[TTSVoice:周启明:happy:[笑]一起去食堂？]
```

插件在原标签位置显示“一起去食堂？”及播放气泡，实际对白按普通文本呈现。旁白保持原样，不要求“人名：”前缀。原始聊天 `mes`、编辑器原文和原始导出都不改；历史发给模型时仍保留标签。显示内容作为纯文本插入，不执行其中的 HTML 或 Markdown。

未绑定音色仍显示完整对白，只跳过合成。显示时去除当前配置的完整语气词标签，以及历史默认四项 `[笑]`、`[叹气]`、`[咳嗽]`、`[清嗓子]`；未知方括号和未知嵌套组保留。发给 TTS 的文本不做这种显示过滤，保留原发声标签。

## 历史兼容的边界

旧回复可能在普通引号对白后紧邻一个重复标签。新模块 `dialogue-render.js` 只检查紧邻段落的引号全文，按顺序合并该段多个引号内容、归一空白并去掉已知发声标签后，完全匹配才省略重复显示、仅添加气泡。

这是保守启发式，不搜索更早段落、不做模糊或语义匹配。文字改写、额外空行等复杂格式可能仍显示两份；完整对白恰好重复也可能被误判。原文始终保留，不会因显示去重而丢失消息。其他正则若提前删除标签，可能影响定位，沿用消息下方语音条作为回退。

## 提示词及设置隔离

默认英文规则保持五条：实际发言只在标签内出现一次，旁白、动作和心理活动在外；姓名一致、情绪自然、语气词可配置，并检查所有发言及最后两次发言。用户、短信、引文、`<w2g>`、`<catsay>`、摘要及状态栏等既有排除规则保留。未绑定人物仍使用 `New`。

稳定模板保留在 `settings.promptTemplate`，实验模板单独使用 `tagRenderPromptTemplate`；首次使用实验默认，保存或恢复实验模板不覆盖稳定模板。其他设置继续共用：语气词列表、音色映射、具名语料库及聊天绑定的修改不会因代码回退而自动撤销。启用有效且非空的聊天语料时仍独立追加第六条。

不要同时注入旧双份规则。新文件 `prompts/TGbreak_原思维-格式_单份对白实验版.txt` 仅调整历史兼容片段内的 TTS 要求，其余模块、宏及检查保留；旧文件未改。英文实验模板在 `prompts/酒馆_TTSVoice_单份对白实验版.txt`。用户保存的预设不会被插件自动改写。

## 试用和回退

稳定版备份位于 `G:\study\AI\breeze-tts\sillytavern-breeze\dist`：`sillytavern-breeze-0.6.1.zip` 是安装包，`sillytavern-breeze-0.6.1-source.zip` 是源码快照。两个分支各有自己的工作目录，无需在稳定目录里切换分支；继续开发实验版时打开实验目录的 `酒馆插件.code-workspace`。

实验包为 `dist/sillytavern-breeze-0.7.0-tag-render.1.zip`。决定试用时，在实验目录显式运行：

```powershell
.\install.ps1 -SillyTavernPath 'G:\study\AI\SillyTavern-Launcher\SillyTavern' -InstallExperimental
```

安装脚本先备份，再替换原同名扩展。不要另取目录名与稳定版并装或同时启用，否则会共用提示词 key、设置和事件。刷新酒馆后生效，从 0.6.1 试用不需要重启 TTS。

回退时从稳定工作区执行其安装脚本，再刷新酒馆；稳定模板保留在原设置键。Git 分支、标签和源码归档保存代码基线，不转换实验期间产生的聊天。稳定版对实验单份回复可能只显示气泡；标签里的原文仍保留，可查看原文或重新使用实验渲染。

## 已验证与待验证

- 70 项单元／DOM 测试、JavaScript 语法检查通过；覆盖显示过滤、未知括号、历史匹配、动态预设与原文保留等。
- 35 项不同 Edge 浏览器用例通过，使用模拟酒馆和演示音频；桌面聊天及 375px 手机预设截图已目视检查。
- 实验安装默认 guard 与显式开关下的 `-WhatIf` 验证通过，没有写入真实酒馆。稳定工作区干净，13 个运行文件与真实酒馆逐字节一致，稳定版 44 文件源码归档已校验。
- 未进行真实 LLM 生成或真实酒馆完整流程验收，不能据此承诺模型每次遵循新格式。标签缺失或不完整时，不猜测补齐普通对白。
